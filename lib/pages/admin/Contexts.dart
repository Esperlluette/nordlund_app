import 'package:flutter/material.dart';

class Contexts extends StatefulWidget {
  const Contexts({super.key});

  @override
  State<Contexts> createState() => _ContextsState();
}

class _ContextsState extends State<Contexts> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Liste des contexts'),
      ),body: null,
    );
  }
}
